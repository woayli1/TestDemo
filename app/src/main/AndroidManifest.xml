<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestDemo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".jump.JumpActivity" />
        <activity android:name=".jump.Jump2Activity" />

        <activity android:name=".okhttp.OkhttpActivity" />
        <activity android:name=".okhttp.Okhttp2Activity" />

        <activity android:name=".okhttp.retrofit.RetrofitActivity" />
        <activity android:name=".okhttp.retrofit.RetrofitConverterActivity" />

        <activity android:name=".media.MediaActivity" />
        <activity android:name=".media.MediaRecordActivity" />
        <activity android:name=".media.VideoPlayActivity" />
        <activity android:name=".media.VideoViewActivity" />
        <activity android:name=".media.SoundPlayActivity" />

        <activity android:name=".mvp.MvpActivity" />

        <activity android:name=".shop.ShopActivity" />
        <activity android:name=".shop.ui.detail.GoodsDetailActivity" />

        <activity android:name=".opengl.OpenglActivity" />
        <activity android:name=".opengl.triangle.TriangleActivity" />
        <activity android:name=".opengl.square.SquareActivity" />
        <activity android:name=".opengl.mosaic.MosaicActivity" />
        <activity android:name=".opengl.live2d.Live2DActivity" />
        <activity android:name=".opengl.brush.BrushActivity" />

        <activity android:name=".opengl.live2d.Live2DBasicActivity" />

        <activity android:name=".canvas.CanvasActivity" />
        <activity android:name=".canvas.RadarActivity" />
        <activity android:name=".canvas.brush.BrushCanvasActivity" />

        <activity android:name=".diyView.DiyViewActivity" />
        <activity android:name=".diyView.FlowLayoutActivity" />
        <activity android:name=".diyView.KoiActivity" />

        <activity android:name=".lazyLoad.LazyLoadActivity" />

    </application>

</manifest>